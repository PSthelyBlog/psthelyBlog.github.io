---
title: "Hello World: Testing the Post Pipeline"
description: "A test post to verify PostLayout, TOC, custom MDX components, code blocks, and Giscus comments."
pubDate: 2026-03-01
category: "article"
tags: ["test", "astro", "mdx"]
author: "Philippe"
featured: true
draft: false
---

Welcome to the first test post on DevLog. This post exists to verify that every Step 3 component works correctly.

## Table of Contents Test

This post has multiple headings so the table of contents should appear. On desktop, it should be in the sidebar. On mobile, it should be inline above the content.

## Callout Components

<Callout type="info">
This is an **info** callout. It should have a blue border and icon.
</Callout>

<Callout type="warning" title="Watch out">
This is a **warning** callout with a custom title.
</Callout>

<Callout type="tip">
This is a **tip** callout. Green border and lightbulb icon.
</Callout>

<Callout type="error">
This is an **error** callout. Red border and X icon.
</Callout>

## Code Blocks

Here is a JavaScript code block. It should have syntax highlighting and a copy button on hover:

```javascript
function greet(name) {
  console.log(`Hello, ${name}!`);
  return { greeting: `Welcome to DevLog` };
}

greet('World');
```

And a CSS example:

```css
.terminal-cursor {
  color: oklch(0.75 0.18 175);
  animation: blink 1s step-end infinite;
}
```

## Tool Rating

<ToolRating rating={8} label="Developer Experience" />

## Support CTA

<SupportCTA />

## Third Heading for TOC

This section exists to ensure the TOC has enough entries (3+ headings required).

### A Nested Subheading

This h3 should appear indented in the TOC.

## Wrapping Up

If you can see this post with all components rendered correctly, Step 3 is working. The Giscus comments section should appear below.
